<% provide(:title, "Distribuidores") %>
<% provide(:meta_description, "Contácta a tu distribuidor Black Brocket ahora mismo") %>

<div class="wrapper wrapper-content">
  <div class="row animated fadeInRight">

    <div class="text-center">
      <h1 class="module-title long-up-2x">Distribuidores Black Brocket</h1>
      <h2 class="module-subtitle">Descubre nuestros distribuidores autorizados cerca de tu ubicación.</h2>
    </div>

    <div class="show-tips-container" style="margin-top: 2em;">
        <% if @distributor %>
          <div class="col-sm-12 m-b-md">
            <%= render partial: "distributor" %>
          </div>
          <br>
          <div class="col-sm-12">
            <%= render partial: "/shared/messages", locals: {messages:@messages, create_message_url: @create_message_url}%>
          </div>
        <% else %>
          <p class="text-center">
            No hay ningún distribuidor que le corresponda atender tus
            pedidos por la población en que te encuentras, sin embargo,
            pronto uno de nuestros asistentes se comunicará contigo para atenderte.
          </p>
        <% end %>
    </div>
    <div class="row">
    		<div class="col-sm-6 col-sm-offset-3">
          <div class="devider m-b-xxl">
            <%= image_tag("divider-down.svg") %>
          </div>
    		</div>
    </div>

  </div>
</div>
<br><br>
