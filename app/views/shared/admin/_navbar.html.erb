<nav class="navbar-default navbar-static-side" role="navigation" style="overflow: auto;">
  <div class="sidebar-collapse">
      <a class="close-canvas-menu"><i class="fa fa-times"></i></a>
      <ul class="nav" id="side-menu">
        <li class="nav-header fixed-image-bg">
          <center>
            <div class="dropdown profile-element">
              <%= image_tag User.getImage(@current_user, :mini), :class => "img-responsive img-rounded img-50" %>

              <% if @current_user.is_admin %>
                <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                  <span class="clear"> <span class="block m-t-xs"><strong class="font-bold"><%= @current_user.username %></strong></span>
                  <span class="text-muted text-xs block">Opciones <b class="caret"></b></span>
                </a>
                <ul class="dropdown-menu animated fadeInRight m-t-xs">
                  <li><%= link_to "Editar mi perfil", edit_admin_worker_path(@current_user.hash_id) %></li>
                  <!--li><%#= link_to "Configuración", admin_configurations_path %></li-->
                  <li class="divider"></li>
                  <li><%= link_to(log_out_path, method: "delete") do %>Cerrar sesión<% end %></li>
                </ul>
              <% else %>
                <a><span class="clear"> <span class="block m-t-xs"><strong class="font-bold"><%= @current_user.username %></strong></span></a>
              <% end %>
            </div>
          </center>
        </li>
        <!--Menu Items-->

        <li><%= link_to(admin_welcome_path) do %><i class="fa fa-home"></i> <span class="nav-label">Inicio</span><% end %></li>
        <% @links.each do |link| %>
          <li>
            <%= link_to(link[:link]) do %>
              <i class="<%= link[:icon] %>"></i>
              <span class="nav-label">
                <%= link[:label] %>
              </span>

              <% if link[:label]=="Preguntas de prod." %>
                <% question_count = ProdQuestion.where(answered: false).size %>
                <% if question_count > 0 %>
                  <span class="label label-danger pull-right"><%= question_count %></span>
                  <!--<span class="badge" style="background-color: #F44336; color: #FEFEFE;">
                    <%= question_count %>
                  </span>-->
                <% end %>
              <% end %>

            <% end %>
          </li>
        <% end %>
        <li style="margin-bottom: 40px;"><%= link_to(log_out_path, method: "delete") do %><i class="fa fa-sign-out"></i> <span class="nav-label">Cerrar Sesión</span><% end %></li>
        <!--End Menu Items-->
      </ul>
  </div>
</nav>
