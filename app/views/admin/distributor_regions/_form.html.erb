<%= form_for(:distributor_region, remote: true, html: {class: 'form-horizontal'}) do |f| %>
<div class="form-group"><label class="col-sm-2 control-label">Estado</label>
    <div class="col-sm-10">
      <span><%= @state.name %></span>
    </div>
</div>
  <%= render partial: "/shared/select_city", locals: { cities: @cities } %>
  <%= f.hidden_field :distributor_id, value: @distributor.id %>

  <%= f.submit "Añadir", class: "btn btn-primary pull-right" %>
<% end %>
