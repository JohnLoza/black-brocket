<%= form_for(:warehouse_product,
    url: admin_prepare_product_for_shipment_path(@warehouse.hash_id, "0x0"),
    remote: true, method: :post, html: {id: "transfer_preparation_form"}) do |f| %>

    <%= f.label :quantity, "Cantidad a transferir" %>
    <%= f.number_field :quantity, { class: "form-control m-b", placeholder: "0",
                                    min: 0, max: 999999, step: 0, required: true} %>

    <%= f.label :batch, "Número de lote" %>
    <%= f.text_field :batch, class: "form-control m-b", maxlenght: 20, required: true %>

    <%= f.hidden_field :name %>

    <%= f.submit "Agregar producto a los preparativos de transferencia", class: "btn btn-primary btn-sm" %>

<% end %>
