<% provide(:title, "Registro") %>
<div class="container" style="margin-top: 80px;">
  <div class="row">
     <div class="col-md-12 text-center">
        <h1 class="module-title long-up-2x">Registro</h1>
     </div>
   </div>

  <div class="show-tips-container">
    <%= render 'landing_page_form' %>
  </div>
</div>

<style>
<% image = WebPhoto.where(name: "LANDING_PAGE").take %>
<% if image.blank? %>
  <% url = image_url "person-woman-coffee-cup-large.jpg" %>
<% else %>
  <% url = image.photo.url %>
<% end %>
body { background: url('<%= url %>') center center / cover no-repeat; }
</style>
