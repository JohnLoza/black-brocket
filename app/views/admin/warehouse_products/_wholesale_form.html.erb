<%= form_for(@warehouse, url: admin_update_wholesale_path(@warehouse.hash_id), method: :put) do |f| %>
  <%= f.label :wholesale, "Mínimo para considerarse mayoreo" %>
  <%= f.number_field :wholesale, {:class => "form-control", min: 0, step: 0.1, required: true} %>
  <%= f.submit "guardar", :class => "btn btn-primary" %>
<% end %>
