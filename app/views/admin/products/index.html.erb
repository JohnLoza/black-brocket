<% provide(:title, "Productos") %>
<%= render partial: "/shared/admin/header_band", locals: {title: "Productos", labels: ['Productos']} %>

<!--Content whit animation-->
<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">

    <div class="row">
      <div class="col-lg-12">
        <div class="ibox-content" style="display: block;">
          <div class="row">
              <div class="col-sm-9 b-r margin-bot-resp"><h3 class="m-t-none m-b">Lista de productos</h3>
                 <p>Aquí puedes realizar diversas acciones como dar de alta un producto, modificar, eliminarlo o simplemente ver su información!!!</p>
                 <p>En la búsqueda, separa los parámetros con "," ejemplo: Choco, P0001 </p>

                 <%= form_for(:product, url: admin_products_path, method: :get) do |f| %>
                    <div class="input-group">

                      <% if params[:product] %>
                         <%= f.text_field :search, value: params[:product][:search], :class => "input form-control",  :placeholder => "Buscar producto...", :maxlength => 50  %>
                      <% else %>
                         <%= f.text_field :search, :class => "input form-control",  :placeholder => "Buscar producto...", :maxlength => 50  %>
                      <% end %>

                      <span class="input-group-btn">
                           <%= f.submit "Buscar".html_safe, :class => "btn btn btn-success" %>
                      </span>
                    </div>
                 <% end %>
              </div>
              <div class="hr-line-dashed pro-mobile"></div>
              <% if @actions["CREATE"] %>
                <div class="col-sm-3 text-center">
                     <h4>Registrar Nuevo</h4>
                     <%= link_to "<i class='fa fa-cube med-icon'></i>".html_safe, new_admin_product_path %>
                </div>
              <% end %>
          </div>
        </div>
      </div>
    </div><br>

    <!--Table-->
    <div class="row">
      <%= render partial: "presentation_table" %>
    </div>
    <!--End Table-->

  </div>
</div>
<!--End content whit animation-->
